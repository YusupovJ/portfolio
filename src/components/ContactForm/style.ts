import styled from "styled-components";

export const Wrapper = styled.div`
	.form-wrapper {
		display: flex;
		flex-wrap: wrap;
		gap: 20px;
		padding: 0px 20px;
		justify-content: flex-end;
	}

	.form-input {
		flex: 1 1 40%;
	}

	.form-textarea {
		flex: 1 1 100%;
	}
`;
